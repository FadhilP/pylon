{
  "name": "pi-heartbeat",
  "version": "0.1.0",
  "description": "Bounded background shell jobs with status and cancellation for Pi",
  "type": "module",
  "keywords": [
    "pi-package"
  ],
  "engines": {
    "node": ">=24.15.0"
  },
  "files": [
    "extensions",
    "src",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "test": "node --test test/*.test.ts",
    "check": "tsc --noEmit"
  },
  "pi": {
    "extensions": [
      "./extensions/pi-heartbeat.ts"
    ]
  },
  "pylon": {
    "settings": "./src/web-settings.ts"
  },
  "dependencies": {
    "pylon-core": "0.1.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-ai": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  },
  "license": "MIT"
}
