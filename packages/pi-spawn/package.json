{
  "name": "pi-spawn",
  "version": "0.1.0",
  "description": "Private subagent threads and first-class spawned sessions for Pi",
  "type": "module",
  "keywords": ["pi-package"],
  "engines": { "node": ">=22.19.0" },
  "files": ["extensions", "src", "README.md", "LICENSE"],
  "scripts": { "test": "node --test test/*.test.ts", "check": "tsc --noEmit" },
  "pi": { "extensions": ["./extensions/pi-spawn.ts"] },
  "pylon": { "settings": "./src/web-settings.ts" },
  "dependencies": { "pylon-core": "0.1.0" },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-ai": "*",
    "typebox": "*"
  },
  "devDependencies": { "typescript": "^5.8.0" },
  "license": "MIT"
}
